# Transformer HTML PPT

A static HTML presentation explaining Transformer with Chinese examples, formulas, and canvas animations.

## Deploy

This repo is ready for Vercel static deployment. Use the repository root as the project root.
